package com.example.nautilius.memorycards;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * Created by nautilius on 04/01/2017.
 */
public class BroadCAstReceiverDemarrage extends BroadcastReceiver {

    public void onReceive(Context context, Intent intent) {
     //   Intent startServiceIntent = new Intent(context, ServiceRappelRevision.class);
     //   context.startService(startServiceIntent);
    }
}
